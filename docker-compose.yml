services:
  dev-db:
    image: postgres:14.2-alpine
    restart: on-failure
    ports:
      - 5434:5432
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=123
      - POSTGRES_DB=nest
    networks:
      - nest-test
networks:
  nest-test:
